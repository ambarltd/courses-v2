<script>
  // Redirect user if not signed-in

  const token = window.localStorage.getItem("session-id");
  const signedIn = typeof token === "string";
  if (!signedIn) {
    window.location.replace("/sign-in");
  }
<script>
